<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Create Booking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/static/form.css">

</head>
<body>
<header>
    <div class="wrapper">
        <div class="logo">
            <a href="index.html">Pensionat</a>

        </div>
        <nav>
            <a href="/customers/create">New Customer</a>
            <a href="#">Rooms</a>
            <a href="/customers">All Customers</a>
            <a href="/booking/create">Kontakt</a>
        </nav>
    </div>
</header>

<h1>Create Booking</h1>

<form th:action="@{/booking/create}" th:object="${booking}" method="post">
    <p>
        <label for="firstName">First Name:</label>
        <input type="text" id="firstName" th:field="*{customer.firstName}" required />
    </p>
    <p>
        <label for="lastName">Last Name:</label>
        <input type="text" id="lastName" th:field="*{customer.lastName}" required />
    </p>
    <p>
        <label for="email">Email:</label>
        <input type="email" id="email" th:field="*{customer.email}" required />
    </p>
    <p>
        <label for="phoneNumber">Phone Number:</label>
        <input type="text" id="phoneNumber" th:field="*{customer.phoneNumber}" required />
    </p>
    <p>
        <label for="checkIn">Check-In Date:</label>
        <input type="date" id="checkIn" th:field="*{checkIn}" required />
    </p>
    <p>
        <label for="checkOut">Check-Out Date:</label>
        <input type="date" id="checkOut" th:field="*{checkOut}" required />
    </p>
    <p>
        <label for="amountPersons">Number of Persons:</label>
        <input type="number" id="amountPersons" th:field="*{amountPersons}" required min="1" />
    </p>

    <button type="submit" href="/booking/create">Create Booking</button>


</form>

</body>
</html>
